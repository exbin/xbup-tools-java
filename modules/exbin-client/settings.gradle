rootProject.name = 'exbin-client'

if (rootProject.path == ':') {
    include ':core:exbin-framework'
    include ':modules:exbin-client-api'
}

apply from: '../xbup-tools-java-modules-settings.gradle'
